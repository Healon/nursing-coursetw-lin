# HANDOFF — 自動化更新完成摘要（2026-07-14 00:44，台北）

> 本文件記錄 2026-07-13～14 的最終實作與實跑結果。整體架構、守則與故障處理仍以
> [`HANDOFF.md`](HANDOFF.md) 與 [`README.md`](README.md) 為準；設計與實作細節請直接參考
> `docs/superpowers/specs/2026-07-13-nursing-update-automation-design.md`、
> `docs/superpowers/plans/2026-07-13-nursing-update-automation.md`，不要在新 session 重造。

## 最終狀態

- 正式專案：`/Volumes/MAC SSD/dev/Projects/nursing-coursetw-lin`
- GitHub：`Healon/nursing-coursetw-lin`，`main` 已推送至 `9f5e9df`，本機與遠端同步。
- GitHub Pages：commit `9f5e9df` 的 `pages-build-deployment` 已成功（run `29267206934`）。
- Workflows：`update-events`、`local-source-freshness`、`pages-build-deployment` 均為 active。
- `com.lin.twna-reminder` 已載入：週日 14:00、15:00；只在使用者明確點擊後開瀏覽器。
- `com.lin.nursing-local-update` 已載入：週日 16:00；最近一次排程 exit 0。
- `com.lin.twna-watch` 是選配即時監看，目前未載入；每週流程不依賴它。

## TWNA 人工另存流程

- 唯一收件匣：`/Volumes/MAC SSD/dev/Projects/nursing-coursetw-lin/download-twna`
- 瀏覽器人工開啟 TWNA 活動報名頁，按 `⌘S`，格式選「網頁，僅 HTML」，存入上述資料夾。
- `twna_watch.py`、`twna_reminder.py`、`local_update.py` 與選配 WatchPaths 已統一使用此路徑。
- 收件匣內容已由 `.gitignore` 排除，不會觸發工作區髒檔護欄。
- 2026-07-14 的 `2026-07-14.html` 已成功解析：42 筆與既有資料重複、2 筆只有月份而跳過、
  新增 0 筆；原檔已歸檔至 `download-twna/twna-imported/2026-07-14.html`。
- `manual_imported_at` 與 `manual_checked_at` 均為 `2026-07-14T00:19:13+08:00`。
- TWNA 仍是零自動網路請求；不可新增 fetch、proxy、偽裝 header/cookie 或 `verify=False`。

## 實際端到端驗收

2026-07-14 00:34 在住宅網路執行：

```bash
.venv/bin/python scripts/local_update.py --no-push
```

結果：

- jct：`status=ok`、19 筆、`last_success=2026-07-14`
- tnpa：`status=ok`、11 筆、`last_success=2026-07-14`
- TWNA：42 筆既有資料，本週已人工確認
- 彙整：113 筆，`overall=ok`
- `scripts/status.py --check` exit 0
- `scripts/check_freshness.py` exit 0
- 完整離線測試：214 passed
- 三份 plist lint、shell syntax、Python compile 與 AST `verify=False` 稽核均通過；
  `verify=False` 呼叫數為 0。

實跑後四個資料產物已提交為 `9f5e9df chore: local sources update (jct/tnpa/twna)` 並推送；
GitHub Pages 部署成功。

## 已落地的執行邊界

- `cloud`：nuna、critical、psy、tnna、tnma、ni、ahqroc、hospice；GitHub 週日 15:00。
- `local`：jct、tnpa；只在台灣住宅 IP 執行，同一天成功後再次按一鍵不重抓。
- `manual`：twna；只讀人工另存頁與 `data/manual_twna.json`。
- 週一 09:00 watchdog 完全離線，要求 jct／tnpa／twna 都在剛開始的星期日週期成功。
- Finder／Dock 一鍵 `scripts/run_local_update.command` 會執行完整本機流程並 commit/push；不要把它
  當成只匯入 TWNA 的工具。只想本機處理另存頁可執行 `.venv/bin/python scripts/twna_watch.py`。

## 下一個 session 建議

1. 先讀本檔、`HANDOFF.md`、`README.md`，再看 GitHub Actions 最近執行狀態。
2. 觀察下一個週日／週一週期：14:00、15:00 提醒；15:00 cloud；16:00 local；週一 09:00 watchdog。
3. 若要「另存後立即匯入」，再評估是否載入 `com.lin.twna-watch`；目前不必為每週流程安裝。
4. 若來源改版，只用 fixture／mock 迭代；未確認根因前不要反覆對外實跑。

## Suggested skills

- `verification-before-completion`：宣稱修復或部署成功前，取得 pytest、health、Git 與 Actions 證據。
- `systematic-debugging`：來源失敗時先區分網站改版、憑證、robots、機房 IP 與本機排程問題。
- `dual-machine-sync`：Mac Mini／MacBook Air 切換前確認 commit、push、pull 與 launchd 主機責任。
- `github:gh-fix-ci`：GitHub workflow 亮紅且確定是 CI 實作問題時使用；不可藉此繞過來源限制。

